HTTP : (초기 목적) 단순 문서전달

웹에서 단순한 문서 전달 이상을 원함
-> 실시간 통신 (ex. 채팅, 게임, 주식가격)

http 통신 특징 : 요청이 있어야만 응답이 가능함!

----------------------------------------------------------

WebSocket

HTTP 통신 개시
클라이언트가 http 요청을 서버로 보냄
-> 실시간 통신하고 싶다. webSocket 통신으로 변환할래?
-> ok -> websocket 통신 (요청, 응답의 순서가 정해질 필요가 없음)
(한명이 위 통신을 끊으면 다시 HTTP 통신으로 변환)

----------------------------------------------------------

websocket 패키지 내 3개 전부 확인...!!