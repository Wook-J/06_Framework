시험 1
원인
(Controller class 선언 상단부에 @RestController 작성 가정)

1. new StringBuilder(requestUrl)에서 requestUrl에 값이 대입되어 있지 않아 불러올 수 없음

2. serviceKey가 URI 뒷부분에 쿼리스트링으로 작성되어야 하는데 작성되어 있지 않음
URLSyntaxException 발생


조치내용
1. [원인 1 관련] String requestUrl = ""; 부분을 아래와 같이 변경
String requestUri = "http://apis.data.go.kr/B552584/ArpltnInforInqireSvc/getCtprvnRltmMesureDnsty"

2. [원인 2 관련] StringBuilder urlBuilder = new StringBuilder(requestUrl); 아래에 다음과 같은 코드 추가
urlBuilder.append("?" + URLEncoder.encode("serviceKey","UTF-8") + "=" + serviceKey); /*Service Key*/

(Controller class 상단부에 @RestController가 아닌 @Controller가 작성된 경우)
public String airPollution( ... ) throws IOException { ... } 상단부에 다음과 같은 어노테이션 추가
@ResponseBody

-----------------------------------------------------------------------------
시험 2
원인
1. requestUrl 부분에 요청주소가 작성되어야 하는데 빈문자열로 되어 있음

2. requestUrl에 필수 입력값인 sidoName이 작성되어 있지 않음

조치내용
1. [원인 1 관련] const requestUrl = ''; 부분을 아래과 같이 변경(문자열 추가하므로 const 아닌 var)
var requestUrl = "http://apis.data.go.kr/B552584/ArpltnInforInqireSvc/getCtprvnRltmMesureDnsty";

2. [원인 2 관련] requestUrl += `&returnType=json`; 아래에 다음과 같은 코드 추가
requestUrl += `&sidoName=${cityName}`;